version: '3'

services:
  db:
    image: postgres
    environment:
      POSTGRES_DB: bakery
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:6002
    volumes:
      - .:/code
    environment:
      DB_HOST: 'db'
    ports:
      - "3000:3000"
      - "6002:6002"
    depends_on:
      - db